/**
 * App brand name shown next to the logo in sidebar, header, login, and register.
 * Chosen to match the logo (barcode + magnifying glass = scan + scope/lookup)
 * and the system: Amazon product scanning, lookup, inventory, labels.
 */
export const BRAND_NAME = 'ScanScope';
